extends base

block vars
  - var title = 'Dashboard'

block body
  h1 Dashboard
  <a href="/logout">Logout</a>
  p.
    Welcome, #{user.username}! You are now logged in.

  h2 User Information
  p Username: #{user.username}
  p Email:       #{user.email}